<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LibrettoUniversitario"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".AddExamActivity"
            android:exported="true" />

        <activity
            android:name=".DatePickerFragment"
            android:exported="true" />

        <activity
            android:name=".Exam"
            android:exported="true" />

        <activity
            android:name=".ExamAdapter"
            android:exported="true" />

        <activity
            android:name=".ExamList"
            android:exported="true" />

        <activity
            android:name=".OnExamDeleteClickListener"
            android:exported="true" />

        <activity
            android:name=".Person"
            android:exported="true" />

        <activity
            android:name=".RegistrationActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustPan|stateHidden"/>

        <activity
            android:name=".ResultActivity"
            android:exported="true" />

        <activity
            android:name=".TopBarActivity"
            android:exported="true" />

    </application>

</manifest>